from setuptools import find_packages, setup

setup(
    name='src',
    packages=find_packages(),
    version='0.1.0',
    description='We are going to use the movielens to build a simple item similarity based recommender system.',
    author='Hesbon',
    license='MIT',
)
